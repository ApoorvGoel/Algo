package InterviewBit;

import java.util.ArrayList;
import java.util.Collections;

/*
 * 
 * Given a list of non negative integers, arrange them such that they form the largest number.

For example:

Given [3, 30, 34, 5, 9], the largest formed number is 9534330.

Note: The result may be very large, so you need to return a string instead of an integer
 */
public class ArraysLargestNumber {

	public static void main(String[] args) {
		ArraysLargestNumber a = new ArraysLargestNumber();
		ArrayList<Integer> A = new ArrayList<Integer>();
		A.add(3);
		A.add(30);
		A.add(34);
		A.add(5);
		A.add(9);
		
		a.getLargestNumber(A);
	}

	public String getLargestNumber(ArrayList<Integer> A){
				
		return null;
	}
	
	public void mergeSort(ArrayList<Integer> A,int l,int r){
		
		if(l>=r){
			return ;
		}
		
		
		
		
	}
	
	
}
